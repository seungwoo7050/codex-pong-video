version: '3.9'
services:
  db:
    image: mariadb:11
    container_name: codexpong-db
    environment:
      - MARIADB_ROOT_PASSWORD=rootpass
      - MARIADB_DATABASE=codexpong
      - MARIADB_USER=codexpong
      - MARIADB_PASSWORD=codexpong
      - TZ=Asia/Seoul
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
    command: ["mysqld", "--character-set-server=utf8mb4", "--collation-server=utf8mb4_general_ci"]

  redis:
    image: redis:7-alpine
    container_name: codexpong-redis
    environment:
      - TZ=Asia/Seoul
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "no"]

  backend:
    build: ./backend
    container_name: codexpong-backend
    environment:
      - DB_HOST=db
      - DB_NAME=codexpong
      - DB_USER=codexpong
      - DB_PASSWORD=codexpong
      - AUTH_JWT_SECRET=change-me-in-prod-secret-please-keep-long
      - TZ=Asia/Seoul
      - APP_STORAGE_ROOT=/app/storage
      - APP_STORAGE_REPLAY_EVENTS=replay-events
      - APP_STORAGE_EXPORT=exports
      - REDIS_HOST=redis
      - SPRING_PROFILES_ACTIVE=dev
    ports:
      - "8080:8080"
    depends_on:
      - db
      - redis
    restart: unless-stopped
    volumes:
      - replay_events:/app/storage/replay-events
      - export_artifacts:/app/storage/exports

  frontend:
    build: ./frontend
    container_name: codexpong-frontend
    environment:
      - VITE_BACKEND_URL=http://localhost:8080
      - VITE_BACKEND_WS=ws://localhost:8080
      - TZ=Asia/Seoul
    ports:
      - "5173:5173"
    restart: unless-stopped

  worker:
    build: ./worker
    container_name: codexpong-worker
    environment:
      - REDIS_HOST=redis
      - APP_STORAGE_ROOT=/app/storage
      - APP_STORAGE_REPLAY_EVENTS=replay-events
      - APP_STORAGE_EXPORT=exports
      - TZ=Asia/Seoul
    depends_on:
      - redis
    volumes:
      - replay_events:/app/storage/replay-events
      - export_artifacts:/app/storage/exports

  nginx:
    build: ./infra/nginx
    container_name: codexpong-nginx
    depends_on:
      - backend
      - frontend
    ports:
      - "80:80"
    restart: unless-stopped

volumes:
  db_data:
  replay_events:
  export_artifacts:
